#include <stdio.h>

int main()
{
    int a;
    scanf("%i", &a);
    char *arr[10] = {"zero", "one", "two", "three", "four", "five", "six", "seven",
    "eight","nine"};
     
    if (a >= 1 && a <= 9)
    {
        printf("%s\n", arr[a]);
    }
    else if (a > 9)
    {
        printf("Greater than 9");
    }
    else
    {
        return 1;
    }
    return 0;
}
